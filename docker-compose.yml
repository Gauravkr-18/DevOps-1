version: '3.9'

services:
  web:
    build: .
    container_name: django_app
    command: python manage.py runserver 0.0.0.0:30
    ports:
      - "30:30"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
    restart: always